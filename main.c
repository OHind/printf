#include "main.h"
#include <stdlib.h>
/**
 * main - the entry point of teh program
 * Return: always successful
 */
int main(void)
{
	int len;

	len = _printf("Print a simple sentence");
	return (len);
}
